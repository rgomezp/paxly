{
  "name": "@potionforge/root",
  "private": true,
  "version": "0.0.1",
  "workspaces": {
    "packages": [
      "apps/*"
    ]
  },
  "scripts": {
    "clean": "yarn workspaces run clean",
    "build": "yarn workspaces run build"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "expo": "~53.0.22",
    "react": "19.0.0",
    "react-native": "0.79.6"
  }
}
